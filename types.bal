public type EncryptionRequest record {
    string encodedData;
};

public type EncryptionResponse record {
    string encodedData;
};

public type DecryptionRequest record {
    string encodedData;
};

public type DecryptionResponse record {
    string encodedData;
};
